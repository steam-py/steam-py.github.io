# Version Guarantees

steam.py follows semantic versioning ([semvar](https://semver.org)) with a public API described by the documentation you are reading here.

## Examples of changes

| Change                                   | Breaking |
| ---------------------------------------- | :------: |
| Adding a new attribute/method to a class |    ❌     |

If a method is broken but fixing it is a breaking change, its not considered a breaking change
